#include <stdio.h>
#include <string.h>

#define MAX 100
typedef struct dictionary {
    char word[MAX]
} dictionary;


int main (){
    dictionary d[MAX];

    int sizeDictionary, sizeCorrect, i;

    for (i=0; i< )
}