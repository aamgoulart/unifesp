
#include <stdio.h>
#include <stdlib.h>
int soma(int v[]);

int main()
{
    int v[10], i, resultado;

    for (i=0; i<10; i++){
        v[i]= i+1;
    }

    resultado = div(v[i]);

    printf("o soma do vetor e %d", resultado);

}

int div (int v[]){
    int i, soma;
        soma = (i + soma (i-1));
    }
    if (v[i]==1){
        return 1;
    } else {
    return (soma / i));
    }
}
